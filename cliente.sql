CREATE table clientes(
id_clientes INT, AUTO INCREMENT, PRIMARY KEY
nome_clientes VARCHAR (64) NOT NULL
email VARCHAR(64) 
senha VARCHAR(16) NOT NULL
Telefone VARCHAR (11)
data_cadastro DATA
data_nascimento DATA
);